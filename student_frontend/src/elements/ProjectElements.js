import styled from "styled-components"

// Syle components for the project page section wrapper

export const ProjectPage = styled.section`
  background: #e4e3e685;
  min-height: calc(100vh - 5rem - 9rem);
`
